version: '3.9'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: rag_db
    ports:
      - '5432:5432'

  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - '6333:6333'
